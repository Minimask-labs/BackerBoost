
export enum Routes {
    home = "/",
    sign_in = "/sign-in",
    sign_up = "/sign-up",
    create_campaign = "/campaigns/create",
    discover = "/discover",
    campaigns = "/campaigns",
}

export enum FormFieldTypes {
    email = "email",
    password = "password",
    text = "text"
}